[Unit]
Description=NSO Flask Application
After=network.target

[Service]
Type=simple
User=root
WorkingDirectory=/opt/nso-flask

ExecStart=/usr/local/bin/gunicorn \
    --workers 2 \
    --bind 0.0.0.0:80 \
    application2:app

Restart=always
RestartSec=3

[Install]
WantedBy=multi-user.target
